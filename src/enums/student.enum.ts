export enum SortDirection {
  asc = 'ASC',
  desc = 'DESC',
}

export enum Sort {
  id = 'id',
  createdAt = 'createdAt',
  fullname = 'fullname',
  birthDate = 'birthDate',
  satScore = 'satScore',
  graduationScore = 'graduationScore',
  phone = 'phone',
  avgScrore = 'avgScore',
}
